<header class="navbar navbar-light sticky-top d-flex justify-content-between align-items-center">
  <%= link_to root_path, class: 'px-3' do %>
    <%= image_tag "logo.webp", alt: "Logo", style: "height: 40px;" %>
  <% end %>
  <div class="d-flex align-items-center">
    <%= button_to change_locale_path, method: :patch, params: { locale: I18n.locale == :'pt-BR' ? 'en' : 'pt-BR' }, class: 'btn btn-outline-primary ms-2' do %>
      <%= I18n.locale == :'pt-BR' ? 'English' : 'Português' %>
    <% end %>
    <%= form_for :logout, url: destroy_user_session_path, method: :delete, html: { class: 'nav-link px-3' } do %>
      <%= submit_tag t('links.logout'), class: 'btn btn-outline-secondary' %>
    <% end %>
  </div>
</header>
